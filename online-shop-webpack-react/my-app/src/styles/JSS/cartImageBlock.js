import { createUseStyles } from "react-jss";

const cartImageBlock = {
  cart__image_block: {
    width: "175px",
    height: "115px",
    textAlign: "center"
  },
  cart__image: {
    maxWidth: "100%",
    maxHeight: "100%"
  }
};

export default createUseStyles(cartImageBlock, {name: 'cartImageBlock', index: 24})
