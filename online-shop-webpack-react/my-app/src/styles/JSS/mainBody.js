import { createUseStyles } from "react-jss";

const mainBody = {
  container: {
    maxWidth: '900px',
    margin: '0 auto',
  }
};

export default createUseStyles(mainBody, {name: 'mainBody', index: 300})
