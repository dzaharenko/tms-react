import { createUseStyles } from "react-jss";

const subMenu = {
  catalog: {
    fontWeight: "normal",
    color: '#FFFFFF'
  }
};

export default createUseStyles(subMenu, {name: 'subMenu', index: 30});
